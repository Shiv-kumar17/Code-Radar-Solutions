#include <stdio.h>

int main() {
    int num1, num2;

    // Reading two space-separated integers
    printf("Enter two integers: ");
    scanf("%d %d", &num1, &num2);
    printf("True")

    return 0;
}
